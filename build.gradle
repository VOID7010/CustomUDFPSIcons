buildscript {
  repositories {
    google()
    mavenCentral()
  }
  dependencies {
    classpath "com.android.tools.build:gradle:8.1.0"
  }
}

// No allprojects { repositories { ... } } — we rely on settings.gradle
tasks.register("clean", Delete) {
  delete(rootProject.buildDir)
}
